# @/docker-compose.yml
services:
  web:
    build: .
    # ▼ 追加: 開発モードで起動させる
    command: npm run dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    env_file:
      - .env
    environment:
      - WATCHPACK_POLLING=true