# @/Dockerfile.dev (ローカル開発用)
FROM node:20-alpine

WORKDIR /app

# 依存関係のインストール
COPY package*.json ./
RUN npm install

# ソースコードをコピー (volumesで同期するので、ビルド時は不要だが一応)
COPY . .

# 開発サーバー起動
CMD ["npm", "run", "dev"]