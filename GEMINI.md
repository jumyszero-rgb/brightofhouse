# Connection-S CMS AI Rules

## Project overview
- このプロジェクトは、SaaS型ヘッドレスCMS「Connection-S CMS」です。
- 技術スタック:
  - konohaVPS
  - Next.js App Router
  - Prisma
  - Supabase PostgreSQL
  - Cloudflare R2
  - BlockNote
  - AI gemini

  ## Mandatory model selection order
- モデルは必ず、その時点で利用可能な中で要件を満たす最小のモデルから使用すること。
- 最初から最新・最上位・最大性能のモデルを選ばないこと。
- 通常作業では、まず Flash 系の最小十分モデルから開始すること。
- 現在の Flash 系モデルで解決できない、または能力不足が明確な場合のみ、Flash 系の次の上位モデルへ1段階ずつ切り替えること。
- Flash 系で解決できない場合のみ、Pro 系の最小十分モデルへ切り替えること。
- Pro 系でも解決できない場合のみ、さらに上位の Pro 系モデルへ1段階ずつ切り替えること。
- モデル変更は飛び級せず、必ず1段階ずつ行うこと。
- モデルを上げる前に、現在のモデルで不足している理由を1〜2行で説明し、ユーザーの確認を取ること。
- 単一ファイルの軽微修正、要約、整理、短い調査では、上位モデルを使わないこと。
- 複数ファイル横断、設計判断、難しい不具合解析、大規模リファクタのときのみ、上位モデルへの切り替えを検討すること。
- 利用可能なモデル名・バージョンは固定せず、その時点の CLI / API の available models を確認して判断すること。


## Mandatory rules
- 大きな変更を行う前に、必ず `docs/ai-context.md` を読むこと。
- 変更した際は必ず進捗を/home/user/projects/connection-s-cms/docs/ai-context.mdに更新すること
- 既存の認証フローを壊さないこと。
- 現状のADMIN認証を崩さないこと
- Prisma schema は必要な場合以外変更しないこと。
- `prisma/schema.prisma` を変更する場合は、理由を説明し、必要な migration 手順も示すこと。
- 大規模なリファクタよりも、最小限で局所的な変更を優先すること。
- 明示的な指示がない限り、既存機能を削除しないこと。
- 大きな実装前には、まず短く計画を要約すること。
- 既存アーキテクチャは、明確な改善理由がない限り維持すること。

## Coding rules
- 既存のファイル構成・命名規則を尊重すること。
- server-safe な実装を優先すること。
- コンテナ内ローカル保存は避けること。
- メディアアップロードは Cloudflare R2 を使用すること。
- データ境界（`ownerId`, `siteId`）を必ず維持すること。
- ファイル参照には可能な限りエイリアス（`@/...` など）を使用すること。
- ロジックや修正箇所には、日本語で分かりやすいコメントを記述すること。
- すべてのコードファイルの最上段に、コメントアウトでファイルパスを記載すること。

## Output rules
- まず変更対象ファイルを示すこと。
- 次に実装内容を簡潔に説明すること。
- 実装後に変更点を要約すること。
- コード出力前に、出力してよいか必ず確認を取ること。
- コードは必ずコードブロック内に出力し、通常会話を混ぜないこと。
- コード途中に内部思考や履歴を書かないこと。
- コード出力時は必ず「テキストファイル」として扱えるコードブロック形式で提示すること。
- コード再出力は原則フルコードとする。ただし末尾追加のみの場合は差分提示でもよい。
- 差分出力時は、どのファイルのどこを変更するのかを明確に示すこと。
- コード生成前に、必ず元ファイルおよび関連ファイルを再参照し、不整合を避けること。

## 対話の鉄則
- ファイルが複数ある場合は、必ず1ファイルずつ提示すること。
- 勝手に話を進めないこと。方針変更、実装方針変更、やり方変更など重要事項は、ユーザーの許可があるまで決定しないこと。
- 常に現在のステップ確認を優先し、ユーザーの合図があるまで次のフェーズへ移行しないこと。
- 利用制限とコストを考慮し、不要な肉付けをせず、必要事項だけを端的に回答すること。
- 毎回の返答の最後に、必ず以下の形式で進捗を明示すること。
  - [済]: 今回解決したこと
  - [進行中]: 今まさに議論していること
- 「ワン・アクション、ワン・トピック」を徹底すること。
- 全体像は1回で示してもよいが、詳細は1項目ずつ提示し、ユーザーが「次」と言うまで深掘りしないこと。
- 方針変更・実装開始・新規ファイル作成・既存ファイル変更の前には、必ず確認を取ること。
- モデル仕様や利用可能バージョンは変動するため、固定認識で回答せず、必要に応じて最新状況を確認すること。
